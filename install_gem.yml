---
- name: Install Module
  community.general.gem:
    name: "{{ module }}"
    state: latest
    install_dir: "{{ opt_dir }}/{{ env }}"
    user_install: false

- name: Create Link
  file:
    src: "{{ opt_dir }}/{{ env }}/bin/{{ cmd }}"
    dest: "{{ bin_dir }}/{{ cmd }}"
    state: link
